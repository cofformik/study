stack segment 
dw 32 dup(0)
stack ends
;guizezi
code segment
assume cs:code
 start: mov ax,8000h
        mov ds,ax
        mov si,0000h
        mov cx,0010h
        mov ax,0000h
    a1: mov [si],ax
        inc si
        inc si
        inc ax
        loop a1
        
        mov ah,4ch
        int 21h
 code ends
 end start
        
 		